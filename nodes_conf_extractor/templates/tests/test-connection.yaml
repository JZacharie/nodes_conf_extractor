apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodes_conf_extractor.fullname" . }}-test-connection"
  labels:
    {{- include "nodes_conf_extractor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodes_conf_extractor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
