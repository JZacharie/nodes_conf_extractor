apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodes_conf_extractor.fullname" . }}
  labels:
    {{- include "nodes_conf_extractor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodes_conf_extractor.selectorLabels" . | nindent 4 }}
